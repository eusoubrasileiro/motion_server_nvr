# copy choosen rsa key auto-created by magisk ssh to PC
# might need to copy using (due permission denied adb not root)
# adb shell "su -c cat /data/ssh/ssh_host_rsa_key" > ssh_host_rsa_key
adb pull /data/ssh/ssh_host_rsa_key.pub .
adb pull /data/ssh/ssh_host_rsa_key .
# set the public key to user root 
cd /data/ssh/root/.ssh/  
touch authorized_keys
cat ../../ssh_host_rsa_key.pub > authorized_keys
# restart sshd server 
/data/adb/modules/ssh/opensshd.init stop
/data/adb/modules/ssh/opensshd.init start
# ssh client use 
# user:root port 22
# for mobaxterm use sftp for ssh-browser-type